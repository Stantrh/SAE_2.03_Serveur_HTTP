Pour la configuration du serveur, si vous voulez configurer le serveur par défaut.
Laissez les champs vides (sans espace)

Concernant ce fichier, la configuration est telle que
root est par défaut, cela signifie que le répertoire contenant les ressources est "Ressources"
accept ne contient pas d'ip réseau acceptées (vous pouvez en mettre sans problème pour tester la méthode de src/ComparateurIP.java)
reject idem
acceslog est par défaut créé dans le dossier Logs
errorlog idem
<webconf>
    <port>80</port> !-- Le port d’écoute du serveur
    <root></root> !-- Le répertoire où se situe les fichiers du site web
    <accept></accept> !-- accepte toute requête provenant du réseau de l’adresse IP donnée
    <reject></reject> !-- refuse toute requête provenant du réseau de l’adresse IP donnée
    <acceslog></accesslog>
    <errorlog></errorlog>
</webconf>